<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Tarefas</title>
    <style>
        body {
            background-color: #333;
            color: white;
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        .container {
            background: #444;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            width: 300px;
        }
        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: none;
            border-radius: 4px;
        }
        button {
            padding: 10px;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            width: 100%;
            margin-top: 5px;
        }
        #addTaskBtn {
            background: #28a745;
            color: white;
        }
        #addTaskBtn:hover {
            background: #218838;
        }
        #removeLastBtn {
            background: #ffc107;
            color: black;
        }
        #removeLastBtn:hover {
            background: #e0a800;
        }
        #clearListBtn {
            background: #dc3545;
            color: white;
        }
        #clearListBtn:hover {
            background: #c82333;
        }
        #taskContainer {
            margin-top: 20px;
            text-align: left;
            background: #222;
            padding: 10px;
            border-radius: 6px;
            min-height: 50px;
            max-height: 200px;
            overflow-y: auto;
            border: 1px solid #555;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Lista de Tarefas</h2>
        <input type="text" id="taskInput" placeholder="Digite uma tarefa...">
        <button id="addTaskBtn">Adicionar Tarefa</button>
        <button id="removeLastBtn">Remover Última Tarefa</button>
        <button id="clearListBtn">Limpar Lista</button>
        
        <!-- Caixa para exibir a lista de tarefas -->
        <div id="taskContainer"></div>
    </div>

    <script src="script.js"></script>

</body>
</html>
